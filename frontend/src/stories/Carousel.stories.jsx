import Carousel from '../components/Carousel';

const meta = {
  component: Carousel,
  tags: ['autodocs']
};

export default meta;

/** This is rendered in the index route primary on the testimony or the results section.*/
export const Default = {
  args: {
    
  }
};