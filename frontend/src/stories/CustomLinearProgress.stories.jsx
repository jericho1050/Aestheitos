import { BorderLinearProgress } from '../components/CustomLinearProgress';

const meta = {
  component: BorderLinearProgress,
  tags: ['autodocs']
};

export default meta;

/**  This is being rendered in the enrolled route,which is used to keep track of how many sections the user has completed  */
export const Default = {};